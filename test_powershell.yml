---
 - hosts: localhost
   tasks: 
     - name: Run a pwsh command
       command: pwsh -Command Write-Output "Hey There" | ConvertTo-JSON
       # args:
         # executable: /snap/bin/powershell
       register: result

     - debug:
         msg: "{{ result }}"
