---
 - hosts: localhost
   tasks: 
     - name: Run a pwsh command
       shell: write-out "Hello World!"
       args:
         executable: /snap/bin/powershell
       register: result

     - debug:
         msg: "{{ result }}"
